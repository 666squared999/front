import React from "react";
import { SearchFilters } from "../components/SearchFilters";

export const Search = () => {
    return (
        <div className="Search">
            <SearchFilters />
        </div>
    );
};
